import cpaStatus from "./claim"
import cpaCode from "./code"
import cpaList from "./list"

class Cpa {
  public list = cpaList
  public status = cpaStatus
  public code = cpaCode
}

export default Cpa
