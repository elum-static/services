import referralCode from "./code"
import referralCount from "./count"
import referralEarned from "./earned"
import referralReward from "./reward"

class Referral {
  public code = referralCode
  public count = referralCount
  public earned = referralEarned
  public reward = referralReward
}

export default Referral
